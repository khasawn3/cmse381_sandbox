name: Deploy Markdown to GitHub Pages

on:
  push:
    branches: ["main"] # Change this to your default branch name
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v5

#       - name: Converts Markdown to HTML
#         uses: jaywcjlove/markdown-to-html-cli@main
#         with:
#           source: README.md
#           output: index.html

#       - name: Setup Pages
#         uses: actions/configure-pages@v6

#       - name: Upload Artifact
#         uses: actions/upload-pages-artifact@v4
#         with:
#           # This uploads the entire repository. If your markdown 
#           # is in a specific folder, change '.' to that folder name.
#           path: '.'

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v5

jobs:
  build:
    steps:
    - uses: actions/checkout@v4
    - uses: wranders/markdown-to-pages-action@v1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        files: |-
          README.md
          test/README.md
          test/nested/readme.md
    - run: cp ./images ./dist/images/
    - uses: actions/upload-pages-artifact@v3
      with:
        path: dist

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
    - uses: actions/deploy-pages@v4
      id: deployment
